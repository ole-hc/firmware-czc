#pragma once

#include "esp_log.h"

class SystemManager
{
private:
    static const char* TAG;
public:
    SystemManager();
    ~SystemManager();
    //void initSystemManager();
    //void closeSystemManager();
};
